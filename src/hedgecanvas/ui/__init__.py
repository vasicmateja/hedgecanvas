"""Streamlit presentation layer: view-models, formatting, and chart-building
helpers for the HedgeCanvas live UI (see ``app.py`` at the repository
root for the actual Streamlit entry point).

No payoff, coverage, or boundary-scope logic lives here -- everything in
this package formats or filters results already produced by
``hedgecanvas.domain`` and ``hedgecanvas.live``.
"""
